# content/ — MDX sources

- `work/*.mdx` — case studies in the field-manual shape: frontmatter (title, role,
  context, when, status, links), then Overview / The system / Results / How it was
  built / Changelog / The lesson.
- `methods/spec-driven-agents.md` — the framework, verbatim. It is a governing document:
  RENDER it (007); never edit it as part of an implementation task. Changes to it are an
  owner decision.
- Numbers in content must match `lib` constants where both exist (counter band vs case
  stats) — one value, two views.
