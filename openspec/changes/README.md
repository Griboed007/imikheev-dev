# Changes 001–011

Each folder holds one `proposal.md` following the anatomy in
`content/methods/spec-driven-agents.md` §3: Why · What Changes · Impact (incl. what is
NOT touched) · Design · Spec Delta (WHEN/THEN scenarios) · Tasks (TDD-ordered, ending in
a manual Live DoD group) · Notes for the implementer (hard fences).

Execute strictly in order. Archive after the live DoD, then start the next.
